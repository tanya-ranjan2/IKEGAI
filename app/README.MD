In Any place where `#<CODEBLOCk> ` is present. That code would be replaced with modeified code

```python
#<CODEBLOCk>
#<CODEBLOCk>
```


celery RUN
```python
celery --app celery_queue worker --loglevel=debug --pool=solo
```